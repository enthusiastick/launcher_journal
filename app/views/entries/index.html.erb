<%= render 'header' %>

<h1>Entries</h1>
<%= link_to 'New Entry', new_entry_path %>

<ol>
<% @entries.each do |entry| %>
  <li><a href="/entries/<%= entry.id %>"><%= entry.title %></a> (<%= entry.prettytime %>)</li>
<% end %>
</ol>
